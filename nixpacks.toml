[phases.setup]
nixPkgs = ["nodejs-18_x", "python311"]

[phases.install]
cmds = [
  "npm install",
  "python -m venv /tmp/venv",
  "/tmp/venv/bin/pip install -r proxy/requirements.txt"
]

[start]
cmd = "bash start.sh"
