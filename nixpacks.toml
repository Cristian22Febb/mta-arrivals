[phases.setup]
nixPkgs = ["nodejs-18_x", "python311", "stdenv.cc.cc.lib"]

[phases.install]
cmds = [
  "npm install",
  "python -m venv /tmp/venv",
  "/tmp/venv/bin/pip install -r proxy/requirements.txt"
]

[start]
cmd = "bash start.sh"
